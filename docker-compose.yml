version: "3.3"
services:
  backend:
    build:
      context: ./backend
    image: openredact-backend
    ports:
      - "8000:8000"
  frontend:
    build: ./frontend
    image: openredact-frontend
    ports:
      - "80:80"
