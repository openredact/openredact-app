import React from "react";
import { Card, Elevation } from "@blueprintjs/core";
import "./Control.sass";

const PreviewControl = () => {
  return <Card className="preview-card" elevation={Elevation.ONE} />;
};

export default PreviewControl;
